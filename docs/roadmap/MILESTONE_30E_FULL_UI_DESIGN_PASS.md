# M30E - Full UI Design Pass

Status: complete when validated and tagged.

Purpose:

Apply a controlled, CSS-first global design pass after the M30A-D visual analytics work.

Scope:

```text
- Polish global layout consistency.
- Tune sidebar and topbar appearance.
- Normalize buttons, pills, cards, shadows, and focus states.
- Reduce table and archive density where safe.
- Improve visual consistency across Overview, Reports, Archive, and Run History.
- Avoid adding new features or new panels.
- Avoid backend changes and chart dependencies.
```

Release tag:

```text
custosops-v0.30.4-full-ui-design-pass
```

Validation:

```text
frontend build
backend tests
platform contract audit
evidence module contract audit
Desktop UI proof with 10 workspaces
UI proof artifact checker
public tracked-file scan
```

Next:

```text
M30F - Visual demo package
```