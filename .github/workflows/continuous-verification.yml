name: Continuous Verification

on:
  push:
    branches:
      - master
      - agent/**
  pull_request:
    branches:
      - master
  workflow_dispatch:

concurrency:
  group: custosops-verification-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  repository-hygiene:
    name: Repository syntax and structure hygiene
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Compile Python source and tests
        shell: bash
        run: |
          set -o pipefail
          python -m compileall -q backend/app backend/tests 2>&1 | tee python-compile.log

      - name: Parse frontend JSON manifests
        shell: bash
        run: |
          set -o pipefail
          {
            python -m json.tool frontend/package.json > /dev/null
            echo "Parsed frontend/package.json"
            python -m json.tool frontend/package-lock.json > /dev/null
            echo "Parsed frontend/package-lock.json"
          } 2>&1 | tee json-validation.log

      - name: Install YAML parser
        run: python -m pip install PyYAML

      - name: Parse GitHub YAML configuration
        shell: bash
        run: |
          set -o pipefail
          python - <<'PY' 2>&1 | tee yaml-validation.log
          from pathlib import Path
          import yaml

          files = sorted(Path(".github").rglob("*.yml")) + sorted(Path(".github").rglob("*.yaml"))
          for path in files:
              with path.open("r", encoding="utf-8") as handle:
                  yaml.safe_load(handle)
              print(f"Parsed {path}")
          print(f"YAML files checked: {len(files)}")
          PY

      - name: Check unresolved merge-conflict markers
        shell: bash
        run: |
          set -o pipefail
          if git grep -n -E '^(<<<<<<< |>>>>>>> )' -- . | tee merge-conflict-markers.log; then
            echo "Unresolved merge-conflict markers found." | tee -a merge-conflict-markers.log
            exit 1
          fi
          echo "No unresolved merge-conflict markers found." | tee merge-conflict-markers.log

      - name: Upload repository hygiene evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: custosops-repository-hygiene
          if-no-files-found: warn
          retention-days: 14
          path: |
            python-compile.log
            json-validation.log
            yaml-validation.log
            merge-conflict-markers.log

  verify-linux:
    name: Linux backend, frontend and browser verification
    needs: repository-hygiene
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install backend dependencies
        working-directory: backend
        run: python -m pip install -r requirements.txt

      - name: Run backend tests
        working-directory: backend
        shell: bash
        run: |
          set -o pipefail
          python -m pytest -q 2>&1 | tee ../backend-test.log

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies deterministically
        working-directory: frontend
        run: npm ci

      - name: Build frontend
        working-directory: frontend
        shell: bash
        run: |
          set -o pipefail
          npm run build 2>&1 | tee ../frontend-build.log

      - name: Install Playwright Chromium
        working-directory: frontend
        run: npx playwright install --with-deps chromium

      - name: Start backend
        working-directory: backend
        run: |
          python -m uvicorn app.main:app --host 127.0.0.1 --port 8000 > ../backend-ci.log 2>&1 &
          echo $! > ../backend-ci.pid

      - name: Wait for backend health
        run: |
          for attempt in {1..60}; do
            if curl --fail --silent http://127.0.0.1:8000/api/health > backend-health.json; then
              cat backend-health.json
              exit 0
            fi
            sleep 1
          done
          echo "Backend did not become healthy."
          cat backend-ci.log || true
          exit 1

      - name: Start frontend
        working-directory: frontend
        run: |
          npm run dev -- --host 127.0.0.1 > ../frontend-ci.log 2>&1 &
          echo $! > ../frontend-ci.pid

      - name: Wait for frontend
        run: |
          for attempt in {1..60}; do
            if curl --fail --silent http://127.0.0.1:5173 > /dev/null; then
              exit 0
            fi
            sleep 1
          done
          echo "Frontend did not become available."
          cat frontend-ci.log || true
          exit 1

      - name: Run Playwright workspace and SOC workflow audit
        working-directory: frontend
        env:
          CUSTOSOPS_BASE_URL: http://127.0.0.1:5173
        run: npm run test:e2e

      - name: Upload Linux verification evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: custosops-linux-verification
          if-no-files-found: warn
          retention-days: 14
          path: |
            backend-test.log
            frontend-build.log
            backend-ci.log
            frontend-ci.log
            backend-health.json
            frontend/playwright-report/
            frontend/test-results/

  verify-windows:
    name: Windows scripts and cross-platform build verification
    needs: repository-hygiene
    runs-on: windows-latest
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Validate PowerShell syntax
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $Failures = @()
          $Files = Get-ChildItem -Path . -Filter *.ps1 -Recurse -File |
            Where-Object { $_.FullName -notmatch '[\\/](node_modules|\.venv|dist|reports)[\\/]' }

          foreach ($File in $Files) {
            $Tokens = $null
            $Errors = $null
            [void][System.Management.Automation.Language.Parser]::ParseFile($File.FullName, [ref]$Tokens, [ref]$Errors)

            if ($Errors.Count -gt 0) {
              foreach ($ParseError in $Errors) {
                $Failures += "{0}:{1}:{2}: {3}" -f $File.FullName, $ParseError.Extent.StartLineNumber, $ParseError.Extent.StartColumnNumber, $ParseError.Message
              }
            }
          }

          "PowerShell files checked: $($Files.Count)" | Tee-Object -FilePath powershell-syntax.log

          if ($Failures.Count -gt 0) {
            $Failures | Tee-Object -FilePath powershell-syntax.log -Append
            throw "PowerShell syntax validation failed."
          }

          "PowerShell syntax validation passed." | Tee-Object -FilePath powershell-syntax.log -Append

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install backend dependencies
        working-directory: backend
        run: python -m pip install -r requirements.txt

      - name: Run backend tests on Windows
        working-directory: backend
        shell: pwsh
        run: |
          python -m pytest -q 2>&1 | Tee-Object -FilePath ../backend-test-windows.log
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies deterministically
        working-directory: frontend
        run: npm ci

      - name: Build frontend on Windows
        working-directory: frontend
        shell: pwsh
        run: |
          npm run build 2>&1 | Tee-Object -FilePath ../frontend-build-windows.log
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Upload Windows verification evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: custosops-windows-verification
          if-no-files-found: warn
          retention-days: 14
          path: |
            powershell-syntax.log
            backend-test-windows.log
            frontend-build-windows.log
